# Байгаль орчны нэгдсэн платформ

Байгаль орчны салбарын 7 хэлтсийн үйл ажиллагааг удирдан зохион байгуулах, хянах,
шийдвэр гаргалтыг дэмжих цогц вэб платформ.

## Стек

- Next.js 16 (App Router, Turbopack) · React 19 · TypeScript
- Tailwind CSS v4 (`@theme inline`, CSS хувьсагчаар токен)
- lucide-react (иконууд) · график нь SVG-ээр гараар (гуравдагч сан нэмээгүй)

## Бүтэц

```
src/
  app/
    layout.tsx              Shell: Header (бүтэн өргөн) + Sidebar + MobileNav
    page.tsx                Ерөнхий самбар
    departments/[slug]/     Хэлтэс тус бүрийн ажлын талбар
    map|reports|alerts|sources|settings/
  components/
    shell/                  header, sidebar, mobile-nav (client)
    wells/                  худгийн самбар: dashboard, map, colors
    charts.tsx              BarChart, RowChart, YearRange (SVG/CSS, сангүй)
    ui/                     panel, data (Stat/Badge/Meter/Sparkbars/Sparkline/KeyValue)
  lib/
    departments.ts          7 хэлтсийн бүртгэл — SLUG, өнгө, модуль, төлөв
    utils.ts                cn(), num(), pct(), огнооны формат
```

## Дизайн зарчим — "хээрийн хэмжих хэрэгсэл"

Энгийн админ загвараас зориудаар холдсон. Дүрмүүд:

1. **Карт дээр сүүдэр байхгүй.** Гүнийг 1px зураас (`border-line`) болон
   суурийн давхаргаар (`paper` → `paper-2` → `paper-hi`) илэрхийлнэ.
   Сүүдэр (`.elevated`) нь ЗӨВХӨН үнэхээр хөвж буй гадаргууд — наалдсан толгой,
   хөвөгч цэс. Accent гэрэлтэлт (`.glow`) нь маш ховор, идэвхтэй/чухал
   элемент дээр л.
2. **Булан бараг хурц** — `rounded-xs` (2px). `rounded-lg/xl/2xl` хэрэглэхгүй.
3. **Бүх өнгө OKLCH дээр.** Хэлтсийн 7 өнгө зөвхөн өнгөний өнцгөөрөө (H)
   ялгаатай, гэрэлтэлт (харанхуйд L 0.83 / гэрэлд 0.505) ба ханалт нь адилхан.
   Шинэ өнгө нэмэх бол зөвхөн H-г сонго — L, C-г бүү өөрчил, эс тэгвээс
   нэг өнгө нөгөөгөөсөө "бохир" харагдана.
   Дохио: `--moss` #4beda6 (үндсэн accent, сайн), `--water` #50d5e2 (ус, сенсор,
   real-time), `--ochre` #f5c057 (анхааруулга), `--clay` #f27c7b (ноцтой эрсдэл),
   `--moss-deep` (бараан ногоон дүүргэлт).
4. **Харанхуй = үндсэн горим.** Хамгийн ард нь **тас хар** (`#000`), түүн дээр
   ногоон-хар давхаргууд (зурвас `#05110c` → карт `#0e1f19` → hover `#162e25`),
   амьд ногоон accent (`#4beda6`). Гэрэл горим нь ижил өнгөний өнцгүүд дээр эсрэг
   гэрэлтэлтээр зохиогдсон, хэрэглэгч зориудаар сонговол л асна.
   Токен: `paper/paper-2/paper-3/paper-hi` (canvas → карт → зурвас → hover),
   `ink/ink-2/ink-3`, `line/line-2`. Суурь өнгө өөрчлөх бол зөвхөн эдгээрийн
   `L`, `H`-г л хөндөнө — бусад бүх зүйл эндээс уламжлагдана.
   `line`/`line-2` нь тунгалаг (`color-mix … transparent`) тул ямар ч
   суурин дээр дагаж тохирно.
   `ink-3` нь 10–11px текстэд ордог тул контраст нь **4.5:1-ээс дээш** байх ёстой
   (одоо харанхуйд 6.5, гэрэлд 4.7). Гэрэлтүүлгийг өөрчилвөл дахин шалга.
5. **Ганц фонт — Inter.** Гарчиг, бие, тоо бүгд нэг гэр бүл. Эрэмбийг зөвхөн
   хэмжээ, жин, нягтралаар гаргана: `.display` (жин 600 + нягт tracking).
   **Бүх тоо** `.num` класстай — тусдаа mono фонт БИШ, `tabular-nums`-аар
   баганаараа эгнэнэ. Шинэ фонт бүү нэм.
6. **`.eyebrow`** — 10px, том үсэг, өргөн зайтай жижиг тэмдэглэгээ. Хэсэг бүрийн дээр.
7. **Өнгө = утга.** Чимэглэлийн өнгө үгүй. Хэлтэс бүр өөрийн `--d-*` өнгөтэй,
   зөвхөн зураас/цэг/икон/`.tinted` дэвсгэр дээр л гарна. Дохио: `--moss` (сайн),
   `--ochre` (анхаар), `--clay` (аюул), `--water` (мэдээлэл).
   Хэлтсийн өнгийг дамжуулах загвар: эцэг элемент дээр
   `style={{ "--tone": "var(--d-oi)" }}`, дотор нь `text-(--tone)`,
   `bg-(--tone)`, `.tinted`.
8. **Өтгөн сүлжээ.** Текст 11–13px. Хоосон зай биш, зураасаар тусгаарлана
   (`divide-line`).
9. **Гэрэл/харанхуй хоёулаа заавал.** Харанхуйн токенууд `globals.css`-д
   **зөвхөн нэг удаа** `:root[data-theme="dark"]` дор бичигдсэн. `layout.tsx`
   доторх скрипт эхний зурагдалтаас өмнө `data-theme`-г үргэлж тавьдаг
   (хадгалсан утга эсвэл системийн горим). Шинэ өнгө нэмвэл гэрэл/харанхуй
   хоёр блокт нэм.
10. **Хоосон төлөв нь дизайны нэг хэсэг.** Дата ирээгүй хэлтэст `.hatch`
    зураастай, тасархай хүрээтэй блок харуулна — "хоосон" биш "хүлээгдэж буй".
11. **`.ruler`** — хэмжигчийн зураас. Хуудасны толгойн доор "хэрэгсэл"-ийн
    сэдлийг сануулна.

## Бүтэц дэх хоёр зүйл

- **Толгой бүтэн өргөнтэй.** Тэмдэг, зам заагч, горимын товч нэг мөрөнд.
  Хажуугийн зурвас түүний доор эхэлнэ (`top-[52px]`).
- **Хажуугийн зурвасын өргөн `--rail-w`** (232–460px). Чирж тохируулна,
  давхар товшихөд 288px руу буцна, `localStorage.railw`-д хадгална.
  Гол агуулга мөн энэ хувьсагчаас уншдаг тул React state дамжуулах хэрэггүй.
  `layout.tsx`-ийн boot скрипт эхний зурагдалтаас өмнө сэргээнэ.

## Хэлтсийн бүртгэл

`src/lib/departments.ts` бол цорын ганц эх сурвалж. Хэлтэс нэмэх/өөрчлөх бол
зөвхөн тэндээс. `status: "live"` болгоход sidebar, ерөнхий самбар, эх сурвалжийн
хуудас бүгд автоматаар шинэчлэгдэнэ.

**⚠️ Тэнд ЗӨВХӨН бодит мэдээлэл байна.** Одоогоор хэлтсээс ирсэн цорын ганц зүйл
нь албан ёсны нэр. Модуль, ажлын урсгал, бүртгэх обьект, товчлол, чиг үүргийн
тайлбар — эдгээрийг **таамаглаж бүү бич**. Урьд нь ингэж бөглөсөн бөгөөд
хэрэглэгч зохиомол гэж үзэн бүгдийг устгуулсан.

`icon`, `tone` нь дизайны сонголт (баримт биш) тул зөвшөөрөгдөнө.

`INTAKE_QUESTIONS` нь **бидний асуух** асуултууд — хэлтсийн тухай нотолсон
баримт биш тул энэ дүрмийг зөрчихгүй.

## Дата

**Зохиомол дата ХЭРЭГЛЭХГҮЙ.** Эх сурвалж ирээгүй хэлтэст ямар ч үзүүлэлт
байхгүй. Жишээ тоо оруулбал бодит гэж эндүүрэгдэх эрсдэлтэй.

Эх сурвалж ирэхэд: `src/lib/<сэдэв>.ts` үүсгэж, `src/app/api/<сэдэв>/route.ts`
-оор үйлчлүүлж, `departments.ts` доторх `status`-ыг `"live"` болгоод тухайн
хэлтэст зориулсан хуудас нэмнэ (`app/departments/<slug>/page.tsx` нь
`[slug]`-ээс давуу эрхтэй).

### Холбогдсон: Ногоон бүсийн хэлтэс — өрөмдмөл худаг

- Эх сурвалж: `hudag_2015_2024_SJ` ArcGIS FeatureServer, 12,672 цэг, 2015–2025.
  `_SJ` хувилбарыг СОНГОСОН учир нь орон зайн нэгтгэлээр `aimag_name`,
  `soum_name` талбартай — "Байршил" чөлөөт бичвэрийг задлах шаардлагагүй.
- [lib/wells.ts](src/lib/wells.ts) бүх хуудсыг татаж (2000-аар), толь бичгээр
  шахаж, ангиллуудыг **тоо буурахаар эрэмбэлнэ**. Эрэмбэ нь тогтмол тул
  индекс → өнгө уялдаа утгатай болно (индекс 0 = хамгийн түгээмэл).
- [api/wells](src/app/api/wells/route.ts) 1 цаг кэштэй, ~500KB буцаана.
  Дэлгэрэнгүйг `api/wells/[oid]` -оор тухай бүрд нь татна.
- Самбар нь **нэг дэлгэцэнд багтана** (ArcGIS Dashboard маяг). Хуудас гүйхгүй;
  жагсаалтууд дотроо гүйнэ. Бүтэц:
  толгой → **шүүлтүүрийн мөр** (унждаг цэстэй товчнууд) →
  зүүн: аймаг / дүүрэг / сум · төв: **индикаторын зурвас + газрын зураг** ·
  баруун: жил / сар / гүйцэтгэгч.
- Газрын зураг дээр **Esri-ийн суурь зургийн цуглуулга** (7 хувилбар,
  анхныхаараа хиймэл дагуул). Томруулах товч байхгүй — дугуй, хос товшилт.
- **Дата дүрслэлийн өнгө ГАНЦ**: `--data` (#00a8e8). Диаграм, бөөгнөрөл, цэг
  бүгд үүнийг хэрэглэнэ. Ангилал бүрийг өөр өнгөөр ялгах гэж БҮҮ оролд —
  урьд нь ингээд дэлгэц өнгө дүүрч, буцаагдсан. Ялгаа нь хэмжээ, эрэмбээр.
- **Засаг захиргааны хил** ([lib/boundaries.ts](src/lib/boundaries.ts)):
  улс / аймаг / сум, харагдах хүрээ (visible range) нь zoom 0 / 5 / 8.
  Улсын хилийн тусдаа давхарга байхгүй тул аймгийн олон өнцөгтөөс гаргана:
  хоёр аймгийн дундах ирмэг ЯГ ХОЁР удаа, гадна ирмэг НЭГ удаа тохиолдоно.
  Үүнд **бүтэн нарийвчлалтай** геометр хэрэгтэй (13MB) — ерөнхийлсөн хувилбар
  дээр хөршийн оройнууд давхцахаа больж, арга ажиллахгүй болно.
- Бүх шүүлтүүр cross-filter: диаграм бүр өөрийнхөө хэмжигдэхүүнийг алгасч
  шүүгддэг тул сонгосны дараа ч бусад багана харагдсаар үлдэнэ.

### MapLibre — хоёр заавал мэдэх зүйл

1. **Worker.** MapLibre v6 worker-ийнхөө хаягийг `import.meta.url`-ээс тооцдог
   тул Next.js багцын дор 404 болно. `scripts/copy-maplibre-worker.mjs`
   (predev/prebuild) файлыг `public/maplibre/` рүү хуулж,
   `setWorkerUrl()`-ээр зааж өгнө. Мөн `prewarm()` дуудна — StrictMode эхний
   зургийг устгахад worker сан унтарч, хоёр дахь зураг цэггүй үлддэг.
   Шинж тэмдэг: **хавтан харагдаад векторын цэг алга**.
2. **Бөөгнөрүүлэлт (cluster).** GeoJSON эх сурвалж `cluster: true`-тэй.
   Бөөгнөрлийн тоог бичихэд **glyph** хэрэгтэй бөгөөд растер хавтан ганцаараа
   өгдөггүй — style дотор `glyphs` заавал заана
   (`fonts.openmaptiles.org`). Радиусыг цэгийн тооны **квадрат язгуураар**
   томруулна: дугуйн талбай тоотой пропорциональ болж зөв уншигдана.
   Товшилт: бөөгнөрөл → `getClusterExpansionZoom` (Promise) → ойртоно,
   дан цэг → дэлгэрэнгүй.
3. **Өнгө.** MapLibre `oklch()` уншиж чаддаггүй. Давхаргын өнгийг
   [wells/colors.ts](src/components/wells/colors.ts) доторх `oklchHex()`-ээр
   hex болгож өгнө. Тэр файл maplibre импортлохгүй — самбар нь мөн ижил өнгийг
   хэрэглэдэг бөгөөд газрын зураг зөвхөн хөтөч дээр ачаалагддаг (`ssr: false`).

## Хэл

Интерфэйс бүхэлдээ монгол (кирилл). Код доторх тайлбар мөн монголоор.
