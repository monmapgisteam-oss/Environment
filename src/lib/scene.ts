/**
 * 3D нэгдсэн торон загварууд (integrated mesh, I3S).
 *
 * Гурван загвар нэг л газрыг хамарна — Хан-Уулын урд, 106.70–106.75°E,
 * 47.81–47.84°N, нийт ~5 × 5 км. Эх сурвалж тэдгээрийг гурав хувааж
 * нийтэлсэн (`morin1`–`morin3`); аль нь аль хэсэг болохыг эх сурвалж
 * хэлээгүй тул нэрийг нь дугаараар үлдээв — таамаглаж нэрлэхгүй.
 *
 * ⚠ Бүгд I3S 1.10, Web Mercator (3857), НЭЭЛТТЭЙ (токенгүй хариулна,
 * 2026-09-17). Хаагдвал `SceneOverlay` алдааг ил хэлнэ.
 *
 * ⚠ Хэлтсийн харьяалал хэрэглэгчээс ирээгүй тул эх сурвалжийн хуудсанд
 * (`lib/sources.ts`) ОРУУЛААГҮЙ — аль хэлтсийнх нь тодорхой болмогц
 * нэмнэ.
 */

import { HOSTING } from "@/lib/portal";

export type Mesh = { id: string; name: string; url: string };

export const MESHES: readonly Mesh[] = ["morin1", "morin2", "morin3"].map((id, i) => ({
  id,
  name: `Торон загвар ${i + 1}`,
  url: `${HOSTING}/Hosted/${id}/SceneServer`,
}));
